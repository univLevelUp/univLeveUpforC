#include <stdio.h>

int main(void)
{
    char *p = "HelloWorld";
    printf("%s \n", p);

    p = "welcom to C world!";
    printf("%s \n", p);

    p = "goodbye";
    printf("%s \n", p);

    return 0;
}